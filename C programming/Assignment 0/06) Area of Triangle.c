#include<stdio.h>
#include<conio.h>
void main()
 {
	int Base = 20, Height = 10, Area ;
	Area = 0.5 * Base * Height;

	printf("Base is = %d and Height is = %d Area of triangle is :: %d ",Base,Height,Area);

}