#include<stdio.h>
#include<conio.h>
void main() 
{
	int length = 20, width = 10, parameter ;
	parameter = 2 * (length + width);

	printf("Area of Rectangle %d and %d  is :: %d ",length,width,parameter);

}